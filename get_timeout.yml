---
- name: Get command_timeout
  hosts: all
  gather_facts: no

  tasks:
    - name: Get command_timeout
      debug:
        msg: "This is command timeout value {{ command_timeout }}"

    - name: Get session_timeout
      debug:
        msg: "This is session timeout value {{ session_timeout }}"
