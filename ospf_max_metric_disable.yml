---
- name: Configure OSPF Max Metric
  hosts: all
  connection: network_cli
  gather_facts: no

  vars:
    ospf_id: 1

  tasks:
    - name: Remove ospf max metric
      ios_config:
        lines:
          - "no max-metric router-lsa"
        parents: "router ospf {{ ospf_id}}"
